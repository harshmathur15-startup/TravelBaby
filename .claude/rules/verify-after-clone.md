# Verify All Pages After Clone

After cloning the template into a new product, run the full build chain (npm install, npm run dev, npm run build) and visually verify all pages render correctly before presenting to Sir. Cloned sites inherit template state — verify nothing broke in the copy.
